<!DOCTYPE html>
<html>
  <head>
    <title>NailedIt</title>
    <%= csrf_meta_tags %>

    <link href="https://fonts.googleapis.com/css?family=Bungee|Montserrat" rel="stylesheet">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  	<div id='topbar'>
  		<h1 id='logo'><a href='/'>nailed it.</a></h1>
      <%= form_tag('/logout' , method: 'post', style: 'right: 1vw', class: 'vertical-center') do%>
        <%= submit_tag("logout") %>
      <% end %>
      <% if @current_user %>
        <h4 style='right: 150px; margin: 0;' class='vertical-center'>Hi, <%= @current_user.username %></h4>
      <% end %>
  	</div>
  	<div class='space'></div>
    <div class='space'></div>
    <div class='space'></div>
    <%= yield %>
    <div class='space'></div>
    <div class='space'></div>
    <div class='space'></div>
  </body>
</html>
